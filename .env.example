# Lightweight Connectome Environment Configuration
# Copy this file to .env and fill in your API keys

# === Anthropic LLM Configuration ===
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# === LLM Provider Options ===
# Use mock provider for testing without API calls
# Set to 'true' to use mock responses instead of real LLM
USE_MOCK_LLM=false

# === Debug Configuration ===
# Debug server for LLM monitoring and inspection
# Accessible at http://localhost:3015
DEBUG_SERVER_ENABLED=true
DEBUG_SERVER_PORT=3015

# === Tracing Configuration ===
# Enable/disable trace logging to files
# Traces are saved to ./traces directory
ENABLE_TRACING=true

# === Example Usage ===
# Test with mock provider: USE_MOCK_LLM=true npm run test:console
# Test with Anthropic: npm run test:console (requires ANTHROPIC_API_KEY)